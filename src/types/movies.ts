export interface DataShapeMovie {
	id: string;
	image: string;
	categories: string[]; //puede tener los stirng que quiera dentro de categories, es decir asignarle las categorias que uno quiera
	title: string;
	director: string;
	release_date: string;
	cast: string;
	crew: string;
	image_sec: string;
	description: string;
}
